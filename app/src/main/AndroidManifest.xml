<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.xcapital.river">

    <application
        android:name=".misc.FontHack"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.SplashActivity"
            android:theme="@style/SecondaryTheme"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.LoginActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/SecondaryTheme"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.SwipeActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.BriefIntroActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.ExpenseDetailsActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>
